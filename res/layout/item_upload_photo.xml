<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <ImageView
        android:id="@+id/item_upload_photo_img"
        android:layout_width="90dp"
        android:layout_height="90dp"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:layout_marginTop="10dp"
        android:scaleType="fitCenter"
        android:src="@drawable/create_guide_photos" />

    <ImageView
        android:id="@+id/item_upload_photo_img_delete"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_alignTop="@+id/item_upload_photo_img"
        android:layout_marginLeft="-15dp"
        android:layout_marginTop="-10dp"
        android:layout_toRightOf="@+id/item_upload_photo_img"
        android:src="@drawable/delete" />

</RelativeLayout>
